specVersion: "v1beta"
serviceId: "shop_be"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "shopmap"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
